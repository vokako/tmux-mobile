/src-tauri/target
/target
/dist
/node_modules
/temp
temp/

# Local scratch / not-in-repo assets
/deploy
/plan.md
/example.txt
/tmuxmobile.svg
/icon_macos.png

# Bundled web fonts — generated/downloaded by scripts/fetch-fonts.sh, kept
# out of git (the CJK subset alone is 5 MB). Run that script after cloning.
/public/fonts/

# Team launcher: Python bytecode cache + any local agora bus state
team/__pycache__/
*.db
*.db-wal
*.db-shm

# Project uses npm; stray pnpm lockfile would create a dual-lockfile conflict
/pnpm-lock.yaml
# Backup left by a git history filter/rewrite — keep locally, never commit
/.git.backup-before-filter/
